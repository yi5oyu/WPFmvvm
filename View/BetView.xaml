<UserControl x:Class="MVVMLoginDB.View.BetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMLoginDB.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Command="{Binding GotoCanvasCommand}" Content="결과" Width="50" Height="50" HorizontalAlignment="Right" VerticalAlignment="Top"/>

        <Button Grid.Column="1" Command="{Binding ReturnUserCommand}" Content="EndBet" Width="50" Height="50" HorizontalAlignment="Right" VerticalAlignment="Top"/>

        <Label Grid.Row="1" Content="베팅" FontSize="25" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        <Label Grid.Row="1" Content="소지금" FontSize="20" HorizontalAlignment="Left" Margin="0,30,0,0" />
        
    </Grid>
</UserControl>
